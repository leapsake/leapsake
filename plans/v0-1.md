# Leapsake v0.1 — the road to shipped apps

> **This is the table of contents for everything that must happen before v0.1, in order.**
> It holds the decisions and the sequence; the work itself lives in the numbered docs below,
> each of which is **deleted the day its work lands**. If you want to know *what to pick up
> next*, this file is the only place that answers it.

**v0.1 is:** signed, installable **mobile** apps a stranger can get from both stores, with a
verified backup story and an account system that cannot strand anyone. *(Desktop was in this
sentence until 2026-08-26; it ships in a later release.)* It is **almost** not feature work —
the product is built, with two deliberate exceptions (08 and 09, below). The posture this must
not break is [`../AGENTS.md`](../AGENTS.md) → *Product posture*.

## The order

Each doc is self-contained and deletable. **Do them in the order of the rows below**; the
dependency is real except where noted — **10 is the one exception**: it is iOS-only, so it does
not wait on 04's Android work and the two can run in either order. (The numbers are allocation ids, never reused, so `git log`
stays unambiguous after a doc is deleted — 08 and 09 were allocated last but ran first.)

| # | Doc | What it delivers | Gates |
|---|---|---|---|
| **04** | [`v0-1_04_mobile-pipeline.md`](./v0-1_04_mobile-pipeline.md) | The Android release target → closed test. **Starts the 14-day Play clock**. iOS half ✅ done | 07 |
| **06** | [`v0-1_06_e2e-and-release-gate.md`](./v0-1_06_e2e-and-release-gate.md) | Automated proof of the crucial flows. The gate half ✅ landed inside `scripts/release/` | 07, 10 |
| **10** | [`v0-1_10_external-testflight.md`](./v0-1_10_external-testflight.md) | `pnpm release beta` → external TestFlight, unattended. The first rung a stranger installs. **Needs 06** — `beta` runs the suite `--strict` | 07 |
| **07** | [`v0-1_07_public-repo-and-submission.md`](./v0-1_07_public-repo-and-submission.md) | Public repo; the mobile stores live | — |

**05 has left the chain.** Desktop packaging, signing and auto-update are deferred past v0.1
*(owner, 2026-08-26)* — the doc survives, unnumbered, as
[`desktop-packaging.md`](./desktop-packaging.md). v0.1 is now **mobile only**, which also
removes desktop's E2E leg from 06 and retires open decision 3.

**Parallel, not in the chain:**

- ✅ **08, local notifications — done, 2026-08-16.** Reminders reach a phone with the app closed, no
  server, no push tokens; verified on both iOS and Android simulators/emulators (permission-once,
  digest and `each` delivery, completion/snooze silencing a pending one, cross-device policy edits).
  The durable *why* — the 30-day horizon, the derived-state rule, the denormalized label — lives in
  [`@leapsake/notifications`](../packages/notifications/README.md); the desktop applier and
  notification privacy levels stay deferred to [`v0-2.md`](./v0-2.md).
- ✅ **The web spike — done, 2026-08-15.** It answered yes to all three of its questions, and the
  three **relay** changes it existed to find are confirmed: CORS + `OPTIONS`, the per-IP login
  budget being wrong for an SSR host, and the append-only log never compacting. `apps/web-spike`
  is deleted (tag `web-spike-final`); what a future web client inherits is
  [`web-client.md`](./web-client.md), which outlives this file. It also produced one
  **product rule** — [`encryption/model.md`](./encryption/model.md) §10.1: web and PWA require a
  sync account and must never hold the only copy, because browser storage stays evictable even
  once installed. The Stage 4 web app itself remains post-launch ([`v0-2.md`](./v0-2.md)).
- ✅ **09, mobile global navigation — done, 2026-08-17.** The **second and last** feature-work
  exception, written down as one for the same reason 08 was: so it cannot be cited as precedent.
  It runs *before* 04 because **04 cuts the first store listing and the first screenshots, and the
  tab bar is what they show** — reshuffling tabs once the Play 14-day clock is running is the one
  window you least want to touch. Four tabs (Home, Search, **New**, Settings/Account), People
  demoted from a tab to a catalog reached from Search, one header drawn by the app on both
  platforms with a title that shrinks rather than hides, and warm surfaces. The durable *why* lives
  in `apps/mobile/app/(tabs)/_layout.tsx` and `components/AppHeader.tsx`; the shell's regression
  gate is `apps/mobile/maestro/global-nav.yaml`. Type and icons were **out of scope on purpose** —
  emoji stay, no font is loaded — so the character pass stays a separate, revertible increment.
- ✅ **Developer accounts — done, 2026-08-19.** Both enrolled (Apple Developer Program, Google
  Play). Apple no longer blocks 07B. What is left of the latency is one clock, below.

## The one long clock left

Most of this is work you control. Two items were calendar time you cannot compress, and they
inverted the obvious ordering. **One is now closed:**

- ✅ **Apple Developer Program enrollment** — enrolled *(owner, 2026-08-19)*. Individual, $99/yr;
  the legal name is the public seller name. iOS submission (07B) is unblocked, and TestFlight is
  live: `0.1.0-alpha.1` reached it 2026-08-25 by hand, `0.1.0-alpha.2` on 2026-08-26 from
  `pnpm release`.
- ⏳ **Google Play production access** — the account exists *(owner, 2026-08-19)*, and the wall it
  guards is untouched: a closed test with **≥12 testers opted in continuously for 14 days**, which
  only starts *once a build is uploaded*. **This is still the critical path, but which wall it is
  is now open** — the rule binds *personal* accounts, and the owner is leaning toward forming the
  legal entity before stable v0.1, which replaces it with a D-U-N-S wait of up to 30 days. Either
  way the latency is calendar time that no engineering shortens; see *Open decisions* 4, and note
  its warning that **uploading from the personal account forecloses the choice**.

⚠️ **Reserve only what you will ship — and the Apple half is now committed.** An App Store Connect
record for "Leapsake" exists *(owner, 2026-08-19)*: the form only, no build, nothing shared.
Apple's transfer criteria require an app to have at least one released version to be transferable,
so this record cannot move to an org account until it ships — freeing the name and re-reserving it
is the only alternative, with a race window in between. Two things follow, both cheap now and
expensive later:

- ✅ **The record's bundle ID is `com.leapsake.app`** *(owner, 2026-08-19)*, matching
  `apps/mobile/app.json`. It is chosen at record creation and cannot be edited afterwards, so the
  iOS identity is now settled. See [04](./v0-1_04_mobile-pipeline.md).
- ✅ **The Play-side *name* is not a race** *(checked 2026-08-26)* — correcting an earlier line
  here that said names are first-come-first-served on both stores. That is true of Apple and
  **not** of Play: Play titles are not exclusively reserved, and two apps may share one as long
  as their package names differ, bounded by the impersonation policy rather than by a
  reservation. Nothing needs claiming to protect it.
- ⚠️ **The Play *package name* is the thing that is claimed, and it is claimed by the first
  upload** — globally unique, permanent, and bound to the uploading account. That is why
  *Open decisions* 4 says not to upload from the personal account while the org question is
  open: the risk is not losing `com.leapsake.app` to a stranger, it is binding it to the wrong
  account of your own.

**08 and 09 both sat in front of 04 on purpose** (both now done, ✅ above), for the same reason
from two directions: 04 is where the first store build and the first store listing are cut, so
anything that changes what the app *is* (08's two OS permissions) or what it *looks like* (09's
tab bar, in every screenshot) is cheaper before that than after it. Landing either after 04 would
have meant a second store-metadata pass in the middle of the Play clock.

```
Accounts     ✅ Apple + Play enrolled 2026-08-19       ┌─ iOS submit ─┐
                                                      │              │
Mobile   ✅08 ─ ✅09 ─ ✅04-iOS ─ 04-Android ─ 14-day closed test ─┴─ 06 ─ 07
Desktop      ⏸ deferred past v0.1 → desktop-packaging.md
Web spike    ✅ done 2026-08-15
```

## The decisions this encodes *(settled 2026-07-21)*

| Decision | Choice | Consequence |
|---|---|---|
| Developer account type | **Individual**, both stores | Cheapest start; legal name is public; Play's 12-tester/14-day wall applies. **Both enrolled 2026-08-19** |
| Eventual org move | **Yes, early** — via app transfer | Preserves installs/reviews/bundle IDs, but **not key custody** — see [`@leapsake/key-custody`](../packages/key-custody/README.md) |
| Bundle IDs | `com.leapsake.app` · `com.leapsake.desktop` | Permanent after first publish. `.app` is form-factor-neutral so a future iPad/watchOS target joins the same Apple record; it also survives an RN→native rewrite. Electron stays a separate identity — Universal Purchase would require the Mac App Store and cost `electron-updater` |
| macOS distribution | **Direct download**, notarized, not Mac App Store | No review latency; auto-update is ours; MAS sandbox pain avoided. **Deferred past v0.1** — see [`desktop-packaging.md`](./desktop-packaging.md) |
| Release gate | **Local script** over `pnpm test:all --strict` | Real keychain, $0, honors testing principle #6; hosted CI is an *additional* PR check. **Built** — it is a preflight inside `scripts/release/`, and `alpha` is exempt from `--strict` |
| Repo visibility | **Public at (or shortly before) v0.1** | Unlocks free macOS Actions runners; requires the pre-flight in 07 first |
| Versioning | **Real semver in the repo, numeric core to the stores** *(owner, 2026-08-25)* | Settled and encoded, superseding "everything stays `0.0.0`". `X.Y.Z-stage.N` in the manifests; `app.config.ts` strips the suffix; build numbers are minutes since 2026-01-01 UTC. See [`../CONTRIBUTING.md`](../CONTRIBUTING.md) → *Versioning and releases* |
| Release flow | **Tags, not branches** *(owner, 2026-08-25)* | `main` is the only long-lived branch; the rung is chosen and the number computed. Supersedes the earlier `dev` + `release/*` intent, which a tag does the job of |
| Notifications in v0.1 | **Yes, mobile only** *(owner, 2026-08-14)* | The first feature-work exception. Every other channel (email above all) is out of scope for v0.1, so without it a reminder cannot reach a user who isn't already in the app — done 2026-08-16, see [`@leapsake/notifications`](../packages/notifications/README.md) |
| Mobile navigation in v0.1 | **Yes, before 04** *(owner, 2026-08-17)* | The second and last. The tab bar is what every store screenshot shows, and 04 cuts the first ones; reshuffling it once the Play clock runs means re-cutting them. Type and icons stayed out of scope, so the character pass can still land whenever — done 2026-08-17 |

**Corollary: keep the org move early.** Blast radius scales with user count at transition.

## Open decisions for owner sign-off

1. ✅ **How much of the E2E catalog gates v0.1, really?** — **settled as drafted**
   *(owner, 2026-08-28)*. The amendment below is now the rule; [`06`](./v0-1_06_e2e-and-release-gate.md)
   → §C carries it.

   **The owner's reasoning, because it decides more than this question does:** *alpha and
   beta can be buggy — aim high, but bugs are acceptable. What must be avoided is potential
   **data loss** once v0.1 is stable, and it is unacceptable at v1.0. Right now the important
   thing is to get this in front of testers, on real devices, and learn how people use it.*

   That is a **ratchet, and its axis is data loss rather than defect count**, which is why it
   settles this cleanly: the deferred flows (7b/7c) are exactly the ones that prove data can
   be *given back*, and the rung table already demands them before `rc` — one rung before
   anything a stranger keeps real records in. Nothing about the ratchet is served by paying
   for them one rung earlier, and the thing it *is* served by — a build in front of people —
   is what beta buys. **The bar rises with the rung; it never falls.**

   ⚠️ **The trade is only honest while the tester notes say so.** The accepted risk below is
   mitigated by one sentence in [`release-notes/what-to-test.txt`](../release-notes/what-to-test.txt)
   telling testers this build is not a sole copy of anything. That file is a **preflight
   requirement**, not decoration: it is the whole of what makes "bugs are acceptable here"
   true rather than merely convenient. Nothing may quietly relax it, and `rc` is where it
   stops being enough.

   **The amendment, as signed off — grade the gate by rung, not only by platform.**
   [`v0-1_06_e2e-and-release-gate.md`](./v0-1_06_e2e-and-release-gate.md) → *The release-gate
   policy* binds the full catalog to *the first release on a platform*. The first rung where
   someone who is not the author installs Leapsake is **external TestFlight** — so as written,
   `pnpm release beta` demands the entire catalog on iOS **and** Android, which is the whole of
   06. [`10`](./v0-1_10_external-testflight.md) depends on 06, so the first beta waits on the
   most expensive test work in the project. That is the wrong order for a rung whose whole
   purpose is to get a build in front of people early.

   The rule stays; **its unit changes from *platform* to *platform × rung***, with the full
   catalog required before **rc** rather than before beta. The graded table is in
   [`06`](./v0-1_06_e2e-and-release-gate.md) → §C; the per-flow column is
   [`testing/crucial-flows.md`](./testing/crucial-flows.md) → *the matrix*.

   **The beta bar — all the net-new work the first external build needs:** catalog Flows
   **1, 2, 3, 4, 5**, their **on-screen assertions only**, green on iOS and Android.
   Deferred to **rc**: Flows **7b/7c** (the recovery doors) and **every out-of-band custody
   assertion**. Flows 6/7a are decision 2 below and ship with sync, not with v0.1.

   **The line this draws:** *beta proves the app does not lose data in normal use; rc proves it
   can give data back when the OS loses the key.* Flow 4 is in the beta bar rather than deferred
   with the other custody work because it is **reachable in a v0.1 build** — a local-only
   account is not behind `multiDevice` (`apps/mobile/app/settings.tsx:118`; the flag's line is
   "the relay, not the login") — and because its failure mode is the app falling over as its own
   store is swapped, which costs a tester everything they typed. *(This also corrects a stale
   note in the catalog: mobile no longer reaches account creation only through the relay-bound
   signup path.)*

   ⚠️ **One line of the draft was wrong, and it is `rc`'s bill, not beta's** *(corrected
   2026-08-28, against the built flows)*. It said Flow 4's phrase capture "needs no new app
   surface". Flow 4 **as built does not capture the phrase at all** — it asserts the grid has a
   24th word and no 25th — and the reveal renders the words as 24 separately-numbered `Text`
   nodes, so capturing them means 24 `copyTextFrom` calls stitched together or a new surface
   exposing the phrase as one string. Nothing at `beta` needs it. **7b at `rc` does**, and that
   is where the cost lands. 7c does not: the password door is answered with the password Flow 4
   already typed.

   ⚠️ **What is not being traded, because [`10`](./v0-1_10_external-testflight.md) warns against
   exactly this:** `--strict` stays strict, and the `e2e` tier must be genuinely `ready` and
   **green** — never blocked-tolerated, never exempted the way `alpha` is. 10's warning is
   against letting beta skip the gate; this changes what the gate *contains*, not whether it
   runs. Every flow stays non-vacuous by sabotage, the standard
   [`apps/mobile/maestro/README.md`](../apps/mobile/maestro/README.md) already holds its flows to.

   **The risk being accepted, stated plainly:** at beta, nothing automated proves the recovery
   doors open, so a tester whose iOS keychain is reset (a device restore does this) could be
   stranded with an unreadable store. The rung-appropriate mitigation is the *What to Test* file
   [`10`](./v0-1_10_external-testflight.md) already requires: say in it that this build is not a
   sole copy of anything. **That trade was weighed and accepted** *(owner, 2026-08-28)*, on the
   reasoning at the top of this entry. The alternative considered and declined was promoting the
   doors into the beta bar — 7c is cheap enough to have been tempting (`dev-clear-dbkey` and the
   `RecoveryGate` both already exist; it needs three `testID`s and a flow), and the answer was
   still that a build in testers' hands now is worth more than proving a door that a beta tester
   was told not to depend on. It is `rc`'s, and `rc` is inside v0.1. **Softening `--strict` was
   never on the table and still is not.**

2. **Flows 6/7a in the v0.1 gate or fast-follow?** (1) is now settled, and it points this the
   same way: both flows need two instances, and neither is on the data-loss ratchet for a
   build that ships with sync switched off. **Flow 6 largely
   answers itself**: *enable sync and pair a second device* exercises relay sync, and v0.1
   ships with `multiDevice` **off** in [`@leapsake/flags`](../packages/flags/README.md) — every
   door to it is shut. A v0.1 user cannot reach that flow, so gating the release on it would
   mean gating on a path the build does not expose, provable only by flipping a flag that
   production does not set. It belongs to the release that turns sync **on**, and until then
   the on-state's coverage is that README's obligation rather than this gate's. Flow **7a**
   (cross-device recovery) is a different case: the feature *is* reachable in v0.1, it is
   simply expensive to automate — two instances — and what it proves overlaps decision 5
   below. Decide 7a with 5, not with 6. See 06.
3. ✅ **Retired 2026-08-26** — *auto-update before or after going public* was a desktop
   question, and desktop left v0.1. It returns with
   [`desktop-packaging.md`](./desktop-packaging.md) → C.
4. **Which Play account publishes v0.1 — and therefore whether the tester wall applies at
   all?** *(reframed 2026-08-26)*

   The 12-testers-for-14-continuous-days wall is **not a law of Play**; it is a rule for
   **personal accounts created after 2023-11-13**, which ours is (2026-08-19). It has no
   documented waiver — testers who lapse do not count, re-opting restarts the clock, and
   production review takes up to a further seven days. **Organization accounts are not subject
   to it** and publish straight to production. *(Google's page defines the rule's scope as
   personal accounts and simply does not discuss orgs; the exemption is consistent across
   secondary sources but is not stated by Google in those words — confirm before relying on
   it.)* Reportedly, since 2026 Google also checks testers genuinely **used** the app, so a
   list of names who install and forget may not clear it either.

   **Owner leaning, 2026-08-26: form the legal entity before the stable v0.1 release**, which
   makes this an org publication and the wall moot. That agrees with the *eventual org move —
   yes, early* row above, and **now is the smallest that move will ever be**: nothing is
   published on Play, so there is nothing to transfer and no key custody to break.

   What it trades, rather than removes:

   - **A D-U-N-S number takes up to 30 days** and an entity must exist to get one — so the
     long pole moves from *recruiting humans* to *paperwork*, and is not obviously shorter.
     It replaces a task that can fail (people lapse) with one that merely waits.
   - **The stores' account types diverge.** Apple is already an individual record that cannot
     transfer until it ships at least one version. Survivable, but it means the org move is
     half-done for a while rather than clean.
   - **The published privacy policy names the publisher**, and it must match the App Store
     seller name. [`../PRIVACY.md`](../PRIVACY.md) says *Joshua Smith* because that is what
     the Apple account is *(owner, 2026-08-28)*. Incorporating changes both, and the policy
     is a **live URL a reviewer reads** — so it is one of the things the org move has to
     update rather than discover later.

   ⚠️ **The sequencing hazard, which is the actionable part:** a package name is claimed on
   Play by the account that **first uploads** it — globally unique and permanent — so
   **uploading `com.leapsake.app` from the personal account to get an alpha out binds it
   there**, and moving it later means Google's app-transfer process rather than a fresh start.
   Note this is about the *package name*, not the title: Play titles are not exclusively
   reserved *(checked 2026-08-26)*, so there is no name to lose to anyone else. So if the org
   is coming, **do not upload anything to Play from the personal account** — build the
   `android` target and test it locally instead. This does not block 04; it blocks the
   *upload*, which is the last step and the only irreversible one.

   **Still yours to decide:** whether the entity lands before v0.1 (leaning yes) or v0.1 ships
   personal-account-first with the wall. If the latter, the 12-tester list is back and should
   start immediately, since it is then the only irreducible calendar latency left.
5. **Does v0.1 ship without merge by recovery phrase?** Deferred while building the merge
   *(owner, 2026-08-08)*. `recoverAccount` refuses a device that already holds an account exactly
   as `joinAccount` does, so merging by phrase needs the same copy-first treatment and is a second
   full flow on both clients. The gap: a user who has their account's **phrase** but not its
   password must recover on their *other* device first — which is fine unless that device is the
   one they lost. **Leaning ship-without** (password-plus-a-second-device covers the realistic
   case, and the flow it would duplicate is the most delicate one we have), but it is a real hole
   in the "cannot strand anyone" promise, so it is your call. Context:
   [`encryption/model.md`](./encryption/model.md) §7.2.2.

## Explicitly not v0.1

Everything else lives in [`v0-2.md`](./v0-2.md) — including work that is *ready* and merely
non-blocking (vCard export, reminder search, dedup quality, the onboarding import nudge). The
rule is strict on purpose: **if it does not gate v0.1, it waits.** That is what keeps this list
short enough to finish.

**08 and 09 are the exceptions, and both are written down as ones** so neither can be cited as
precedent.

**08** because a reminders app with no way to reach a user outside the app is not shippable, and
email — the obvious alternative channel — is itself out of scope for v0.1. Notification *privacy
levels* and the *desktop* applier both stayed behind the rule and remain deferred to
[`v0-2.md`](./v0-2.md).

**09** because the tab bar is in every store screenshot and 04 cuts the first ones. Note what it
did *not* do, which is what keeps it an exception rather than a crack: it changed navigation and
surface colour and stopped there. Typography, an icon set, and the illustrated empty states are
untouched and remain [`v0-2.md`](./v0-2.md)'s *styling / the design system* — a screenshot is
cheap to re-cut for a nicer typeface, and expensive to re-cut for a different set of tabs.

**Multi-device sync is built and held back** *(owner, 2026-08-21)*, not unbuilt. The relay, the
join, the merge, and the scheduler all work; v0.1 simply does not show them, behind
`multiDevice` in [`@leapsake/flags`](../packages/flags/README.md). This is the one entry here
that is a *switch* rather than a scope line, so it carries a switch's obligations: the gates are
listed in that README, the on-state stays under test, and the removal is a v0.2 item rather than
a thing to remember. Turning it back on for a session is
`LEAPSAKE_FLAGS=multiDevice pnpm desktop`.

**Out of scope entirely for v0.1:** Windows and Linux desktop, matching the E2E host policy —
blocked, not waived. Windows code-signing certs carry their own procurement latency and, for
OV/EV, hardware-token logistics.
