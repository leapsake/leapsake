# Historical findings that have been read and accepted. One line per finding, by
# fingerprint (commit:file:rule:line), so the same file failing on a *different* secret
# still stops the scan. Anything permanent belongs in `.gitleaks.toml` instead.
#
# Nothing may be added here without a note saying what the secret is, why it is dead,
# and who checked. This file is read by strangers once the repo is public: it is the
# record of what was found in the history, not a place to make findings go away.

# --- Next.js server-actions encryption key, in build output committed by accident ---
# `apps/client-web` was a Next.js app scaffolded in April 2025 and since deleted; it does
# not exist at HEAD. Its `.next/` build directory was committed by mistake and removed
# three commits later ("Remove .next directory from version control"). `encryptionKey` is
# generated by Next.js at build time to encrypt server-action closure arguments, so it is
# only meaningful to a running server built from that exact output. No such server was
# ever deployed and the app is gone. Read against the blobs, not inferred — 2026-09-09.
04c2acda93f4de3e540fa23021546fea9c8d679a:apps/client-web/.next/cache/.rscinfo:generic-api-key:1
04c2acda93f4de3e540fa23021546fea9c8d679a:apps/client-web/.next/server/server-reference-manifest.json:generic-api-key:4
12bcb7baf221305f23746e847f4e881b0fc675e1:apps/client-web/.next/cache/.rscinfo:generic-api-key:1
12bcb7baf221305f23746e847f4e881b0fc675e1:apps/client-web/.next/server/server-reference-manifest.json:generic-api-key:4
4e31151c59d82db6e7ab923caa6afb00c86a1022:apps/client-web/.next/cache/.rscinfo:generic-api-key:1
4e31151c59d82db6e7ab923caa6afb00c86a1022:apps/client-web/.next/server/server-reference-manifest.json:generic-api-key:4
ac7165fabda2efa46819d77206f4e4f6832aa6f1:apps/client-web/.next/cache/.rscinfo:generic-api-key:1
ac7165fabda2efa46819d77206f4e4f6832aa6f1:apps/client-web/.next/server/server-reference-manifest.json:generic-api-key:4

# --- Not a secret at all: a crate name in a Rust lockfile ---
# Line 890 of the `Bootstrap iroh` spike's Cargo.lock is the string `crypto_secretbox`
# inside a `dependencies = [...]` list — a public crates.io package name that trips the
# generic rule on the word "crypto" plus entropy. The neighbouring `checksum = ...` lines
# are public crates.io digests. Cargo.lock does not exist at HEAD. Checked 2026-09-09.
34c3d1cea193de4508033fbd9c5742ab08c83831:Cargo.lock:generic-api-key:890

# ─────────────────────────────────────────────────────────────────────────────────────
# `--all-objects` mode fingerprints for the same findings above.
#
# The deep scan reads blobs rather than diffs, so it fingerprints by blob SHA instead of
# by commit — content-addressed, so these entries cannot drift onto different bytes. They
# are listed separately because the deep scan sees *more* than the gate does: three
# Cargo.lock revisions rather than the one commit that added the line, and every object
# whether or not a ref still points at it. Same three judgments as above, nothing new —
# verified 2026-09-09 by mapping each blob back to the path that held it.

# Cargo.lock revisions — the crate name `crypto_secretbox`, not a secret.
25de3323a1dfc45a288d9b5f11c7ad862394e3a9:generic-api-key:922
50a0102386e10512de4e16db5471c4368e773fb2:generic-api-key:890
60a1c050c15878d4bb3cc20369c8854387f58306:generic-api-key:922

# The two distinct Next.js build keys, each in both files that carried it.
8ca5d5a7fb0a29bbc4f01bdc34a69bc3a63585f4:generic-api-key:1
c3e489ee964e6a64498ea95848adb904916d41f9:generic-api-key:4
e67a8cc7a33f3ba3cb5f27c214c571d9006730c3:generic-api-key:1
f554f407e101cc004f4771e059be06344703e02e:generic-api-key:4

# The localhost test fixture. `.gitleaks.toml` allowlists it by path, which the deep scan
# cannot use — its "path" is a SHA — so it needs an entry here too. Being content-
# addressed, regenerating the fixture produces a new blob that must be judged afresh,
# which is the right outcome. Verified identical to the file at HEAD.
d8f2557c24d36e898602c1da2e322b1dcaab1627:private-key:6
