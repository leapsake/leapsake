import type { Crumb } from "@leapsake/ui/web";

/** The root crumb; which route counts as home is this client's choice. */
export const homeCrumb: Crumb = { label: "People & Pets", href: "/people" };
