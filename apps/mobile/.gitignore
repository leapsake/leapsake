# Native projects generated by `expo prebuild` — app.json + config plugins are the
# source of truth, so these are regenerable and stay untracked (managed-workflow
# feel preserved). Regenerate with `pnpm --filter @leapsake/mobile exec expo prebuild`.
/ios/
/android/

# Expo
.expo/
expo-env.d.ts
